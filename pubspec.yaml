name: bottom_sheet_bar
description: A toolbar that aligns to the bottom of a widget and expands into a bottom sheet.
repository: https://github.com/truthmast/bottom_sheet_bar
version: 1.0.0+3

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.9.0
  flutter_test:
    sdk: flutter
